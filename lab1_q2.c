#include <stdio.h>
#include "header1.h"

int main() {
  int T;
  scanf("%d", &T);

  for (int case_index = 0; case_index < T; case_index++) {
    int N;
    scanf("%d", &N);
    
    int *a1;
    a1 = (int*) malloc(sizeof(int) * N);

    int a, b;
    scanf("%d %d", &a, &b);

  }

  return 0;
}